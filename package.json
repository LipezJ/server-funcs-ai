{
  "name": "ai-workers",
  "workspaces": {
    "packages": [
      "frontend",
      "backend"
    ]
  },
  "scripts": {
    "dev-front": "npm run dev --workspace=frontend",
    "build-front": "npm run build --workspace=frontend",
    "dev-back": "cargo run --workspace=backend",
    "build-back": "cargo build --release --workspace=backend"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.3"
  }
}
